apiVersion: v1
kind: Service
metadata:
  name: {{ include "postrust.fullname" . }}
  labels:
    {{- include "postrust.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "postrust.selectorLabels" . | nindent 4 }}
